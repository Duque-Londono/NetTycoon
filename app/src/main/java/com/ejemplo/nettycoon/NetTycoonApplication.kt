package com.ejemplo.nettycoon

import android.app.Application

/**
 * Clase Application de NetTycoon.
 *
 * Por ahora está vacía a propósito (PASO 1: esqueleto). En pasos posteriores aquí se
 * inicializará lo que deba vivir durante toda la vida del proceso (p. ej. la base de
 * datos Room, el contenedor de dependencias, etc.).
 */
class NetTycoonApplication : Application()
